name: GitHub Actions Demo
on: [pull_request]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🔎 The name of your target branch is ${{ github.base_ref }}."
      - run: echo "🔎 The name of your target branch is ${{ github.head_ref }}."
      - name: Check out code.
        uses: actions/checkout@v3
      - run: git log "origin/${{ github.head_ref }}.."
      - run: cargo install conventional_commits_linter
      - run: RUST_LOG=trace conventional_commits_linter --allow-angular-type-only --from-reference "origin/${{ github.head_ref }}"
